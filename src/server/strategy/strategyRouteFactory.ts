import{NextRequest,NextResponse}from"next/server";import{executeStrategyDecision,type StrategyDecisionOperation}from"./strategyDecisionRuntime";export function strategyPost(operation_id:StrategyDecisionOperation){return async(r:NextRequest)=>{const current=r.headers.get("x-correlation-id"),correlation_id=current&&current.trim()?current:crypto.randomUUID(),payload=await r.json().catch(()=>null),result=await executeStrategyDecision({operation_id,correlation_id,payload});if(!result.ok)return NextResponse.json({reason_code:result.reason_code,correlation_id},{status:result.status,headers:{"x-correlation-id":correlation_id,"cache-control":"no-store"}});return NextResponse.json(result.value,{status:200,headers:{"x-correlation-id":correlation_id,"cache-control":"no-store"}});};}
