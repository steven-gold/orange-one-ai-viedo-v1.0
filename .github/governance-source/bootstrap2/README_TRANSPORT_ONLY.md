# Bootstrap transport only

This directory is non-normative transport used only to reconstruct the verified 75-file governance source package. It MUST NOT be used as Current Specification authority or by normal Stage loaders. The active materialized source is `.github/governance-source/active/source/` after successful reconstruction, exact file-count verification, and checksum verification. Bootstrap transport must be removed after materialization closure.
