name: Blueprint Traceability Governance Promotion
on:
  push:
    branches:
      - rebuild-v2.1.1
    paths:
      - '.github/governance-maintenance/BLUEPRINT_TRACEABILITY_PRECHECK_TRIGGER'
      - '.github/governance-maintenance/BLUEPRINT_TRACEABILITY_PROMOTE_TRIGGER'
permissions:
  contents: write
jobs:
  blueprint-traceability-governance:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install PyYAML==6.0.2
      - name: Execute governance precheck or promotion
        shell: bash
        run: |
          gzip -dc .github/governance-maintenance/promote_blueprint_traceability_governance.py.gz > /tmp/promote_blueprint_traceability_governance.py
          if [ -f .github/governance-maintenance/BLUEPRINT_TRACEABILITY_PROMOTE_TRIGGER ]; then
            python /tmp/promote_blueprint_traceability_governance.py --mode promote
          else
            python /tmp/promote_blueprint_traceability_governance.py --mode precheck
          fi
