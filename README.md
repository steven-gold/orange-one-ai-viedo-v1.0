# ACPOS Immutable Governance Ruleset

This branch contains only governance definitions, lifecycle rules, invariant registries, and product-neutral rule validators.

- Governance/rules authority: `rebuild-v2.1.1`
- Product/page execution workline: `0921acpos`
- Product sources, Active Work Units, execution state, Stage evidence, product runners, and page outputs MUST NOT be stored on this branch.
- After this freeze commit, this branch is immutable by project policy. Any future product execution MUST occur on `0921acpos`.
