import{NextResponse}from"next/server";import type{IamOperation}from"@/domain/iam/iamRuntimeContract";import{runIamOperation}from"@/server/iam/iamRuntime";function correlation(req:Request){return req.headers.get("x-correlation-id")||crypto.randomUUID();}async function body(req:Request){return req.json().catch(()=>undefined);}export function createIamPostRoute(operation:IamOperation){return async(req:Request,ctx?:{params:Promise<Record<string,string>>})=>{const params=ctx?await ctx.params:{};const result=await runIamOperation({operation,correlation_id:correlation(req),account_id:params.accountId,resource_id:params.id,draft_id:params.id,payload:await body(req)});return NextResponse.json(result,{status:result.ok?200:503});};}export function createIamPatchRoute(operation:IamOperation){return createIamPostRoute(operation);}export function createIamDeleteRoute(operation:IamOperation){return createIamPostRoute(operation);}