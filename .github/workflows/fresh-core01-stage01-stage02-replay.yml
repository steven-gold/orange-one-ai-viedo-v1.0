name: Fresh CORE-01 Stage01 Stage02 Replay

on:
  push:
    branches: [rebuild-v2.1.1]
    paths:
      - '.github/governance-maintenance/run_fresh_core01_stage01_stage02_replay.py'

permissions:
  contents: write

jobs:
  fresh-replay:
    if: "${{ github.event.head_commit.message == 'test(core01): stage clean Stage-01 Stage-02 fresh replay R2' }}"
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONPYCACHEPREFIX: /tmp/acpos-governance-pycache
      ACPOS_RUN_ROOT: 00_SOURCE_INTAKE/fresh_run_005
      STAGE02_PAGE_SCOPE: CORE-01
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rebuild-v2.1.1
          fetch-depth: 0
      - name: Install verifier dependency
        run: python -m pip install --disable-pip-version-check 'PyYAML==6.0.2'
      - name: Assert exact authorization and Current governance
        shell: bash
        run: |
          set -euo pipefail
          test -f governance/test/spec_change_authorizations/USR-DIRECTIVE-20260919-RERUN-CORE01-STAGE01-STAGE02-R2.yaml
          grep -q 'GOV-REV-20260919-PROFILE-TOKEN-DECONTAMINATION-HARDENING' governance/specifications/REGISTRY.yaml
          test -d 00_SOURCE_INTAKE/fresh_run_004
          test -f 00_SOURCE_INTAKE/fresh_run_004/00_SOURCE_INTAKE/RAW_SOURCE/CORE-01/CORE_PAGE_VISUAL_AUTHORITY_FINAL_SCRIPT_CONTENT_CLOSED.yaml
          test ! -e 00_SOURCE_INTAKE/fresh_run_004/00_SOURCE_INTAKE/RAW_SOURCE/ASSET-01
      - name: Compile fresh replay producer and provenance finalizer
        run: |
          python -m py_compile .github/governance-maintenance/run_fresh_core01_stage01_stage02_replay.py
          python -m py_compile .github/governance-maintenance/finalize_fresh_core01_stage01_stage02_r2.py
      - name: Clear prior Stage-01 Stage-02 data and execute fresh CORE-01 replay
        run: python .github/governance-maintenance/run_fresh_core01_stage01_stage02_replay.py
      - name: Verify exact cleanup and replay boundary
        shell: bash
        run: |
          set -euo pipefail
          test ! -e 00_SOURCE_INTAKE/fresh_run_004
          test -d 00_SOURCE_INTAKE/fresh_run_005
          test ! -e 00_SOURCE_INTAKE/fresh_run_005/00_SOURCE_INTAKE/RAW_SOURCE/ASSET-01
          test ! -e 00_SOURCE_INTAKE/fresh_run_005/01_CLASSIFIED/ASSET-01
          test ! -e 00_SOURCE_INTAKE/fresh_run_005/02_BASE_BLUEPRINT/ASSET-01
          test ! -e 00_SOURCE_INTAKE/fresh_run_005/03_BLUEPRINT_BINDING/ASSET-01
          test ! -e 00_SOURCE_INTAKE/fresh_run_005/04_PAGE_FUNCTIONAL_CONTRACT/ASSET-01
          test ! -e governance/test/history/stage02
          git diff --check
      - name: Upload fresh Stage-01 Stage-02 evidence
        id: evidence
        uses: actions/upload-artifact@v4
        with:
          name: core01-stage01-stage02-fresh-run-005
          if-no-files-found: error
          retention-days: 30
          path: |
            00_SOURCE_INTAKE/fresh_run_005
            .github/stage02-test/STAGE02_ACTUAL_TEST_RESULT.json
            governance/test/stage02
      - name: Bind exact workflow and artifact provenance
        env:
          FRESH_REPLAY_RUN_ID: "${{ github.run_id }}"
          FRESH_REPLAY_SOURCE_SHA: "${{ github.sha }}"
          FRESH_EVIDENCE_ARTIFACT_ID: "${{ steps.evidence.outputs.artifact-id }}"
          FRESH_EVIDENCE_ARTIFACT_DIGEST: "${{ steps.evidence.outputs.artifact-digest }}"
        run: python .github/governance-maintenance/finalize_fresh_core01_stage01_stage02_r2.py
      - name: Validate current-attempt projection before persistence
        run: |
          python governance/ci/validate_validation_remediation_closure_protocol.py
          python governance/ci/validate_governance_portability.py
          python governance/ci/validate_profile_history_binding.py
      - name: Verify transaction hygiene
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          test ! -d governance/test/temporary
          git status --short
      - name: Persist clean fresh CORE-01 Stage-01 and Stage-02 execution
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'steven-gold'
          git config user.email 'proorangeone@gmail.com'
          git add -A
          test -n "$(git diff --cached --name-only)" || { echo 'BLOCK: no fresh replay changes staged' >&2; exit 1; }
          git commit -m "test(core01): persist clean Stage-01 Stage-02 fresh replay R2" \
            -m "Execution-Authorization: USR-DIRECTIVE-20260919-RERUN-CORE01-STAGE01-STAGE02-R2" \
            -m "Scope: CORE-01 only; ASSET-01 remains absent; exact workflow/artifact provenance bound"
          git push origin HEAD:rebuild-v2.1.1
