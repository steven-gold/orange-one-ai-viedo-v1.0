import type{SysServiceOperation,SystemContinuityContext}from"@/domain/system/systemRuntimeContract";export type SysRequest={operation_id:SysServiceOperation;correlation_id:string;context:SystemContinuityContext;payload?:unknown};export type SysBindings={resolveContinuityContext:(system_change_id:string)=>Promise<SystemContinuityContext>;authorize:(r:SysRequest)=>Promise<{allowed:true}|{allowed:false;reason_code?:string}>;execute:(r:SysRequest)=>Promise<unknown>;audit:(e:SysRequest&{outcome:"ALLOWED"|"DENIED"|"SUCCESS"|"ERROR";reason_code?:string})=>Promise<void>};let binding:SysBindings|null=null;export function configureSystemLifecycleRuntime(n:SysBindings){binding=n;}async function audit(b:SysBindings,e:Parameters<SysBindings["audit"]>[0]){try{await b.audit(e);}catch{}}export async function executeSystemLifecycleOperation(input:{operation_id:SysServiceOperation;correlation_id:string;system_change_id:string;payload?:unknown}){const b=binding;if(!b)return{ok:false as const,reason_code:"SYS01_RUNTIME_NOT_BOUND",correlation_id:input.correlation_id};const context=await b.resolveContinuityContext(input.system_change_id).catch(()=>null);if(!context)return{ok:false as const,reason_code:"SYSTEM_CONTINUITY_CONTEXT_UNRESOLVED",correlation_id:input.correlation_id};const r={operation_id:input.operation_id,correlation_id:input.correlation_id,context,payload:input.payload};const a=await b.authorize(r).catch(()=>({allowed:false as const,reason_code:"AUTHORIZATION_EVALUATION_FAILED"}));if(!a.allowed){const reason_code=a.reason_code??"PERMISSION_OR_GATE_DENIED";await audit(b,{...r,outcome:"DENIED",reason_code});return{ok:false as const,reason_code,correlation_id:input.correlation_id};}await audit(b,{...r,outcome:"ALLOWED"});try{const value=await b.execute(r);await audit(b,{...r,outcome:"SUCCESS"});return{ok:true as const,value,correlation_id:input.correlation_id};}catch{await audit(b,{...r,outcome:"ERROR",reason_code:"SYS01_SERVICE_OPERATION_FAILED"});return{ok:false as const,reason_code:"SYS01_SERVICE_OPERATION_FAILED",correlation_id:input.correlation_id};}}
