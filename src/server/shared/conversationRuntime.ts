export type ConversationOperationId="sendConversationMessage"|"stopConversationGeneration";export type ConversationRequest={operation_id:ConversationOperationId;correlation_id:string;conversation_id:string;payload:unknown};export type ConversationBindings={authorize:(r:ConversationRequest)=>Promise<{allowed:true}|{allowed:false;reason_code?:string}>;execute:(r:ConversationRequest)=>Promise<unknown>;audit:(e:ConversationRequest&{outcome:"ALLOWED"|"DENIED"|"SUCCESS"|"ERROR";reason_code?:string})=>Promise<void>};let binding:ConversationBindings|null=null;export function configureConversationRuntime(n:ConversationBindings){binding=n;}async function audit(b:ConversationBindings,e:Parameters<ConversationBindings["audit"]>[0]){try{await b.audit(e);}catch{}}export async function executeConversationOperation(r:ConversationRequest){const b=binding;if(!b)return{ok:false as const,status:503,reason_code:"CONVERSATION_RUNTIME_NOT_BOUND",correlation_id:r.correlation_id};let a:Awaited<ReturnType<ConversationBindings["authorize"]>>;try{a=await b.authorize(r);}catch{return{ok:false as const,status:403,reason_code:"AUTHORIZATION_EVALUATION_FAILED",correlation_id:r.correlation_id};}if(!a.allowed){const reason_code=a.reason_code??"PERMISSION_OR_SCOPE_DENIED";await audit(b,{...r,outcome:"DENIED",reason_code});return{ok:false as const,status:403,reason_code,correlation_id:r.correlation_id};}await audit(b,{...r,outcome:"ALLOWED"});try{const value=await b.execute(r);await audit(b,{...r,outcome:"SUCCESS"});return{ok:true as const,value,correlation_id:r.correlation_id};}catch{await audit(b,{...r,outcome:"ERROR",reason_code:"CONVERSATION_OPERATION_FAILED"});return{ok:false as const,status:503,reason_code:"CONVERSATION_OPERATION_FAILED",correlation_id:r.correlation_id};}}
