export type SocNormalizedProjection={page_state:string|null;values:Readonly<Record<string,string>>;gate_state:Readonly<Record<string,boolean>>};
export type SocProjectionResolver={resolve:(raw:unknown)=>SocNormalizedProjection|Promise<SocNormalizedProjection>};
let resolver:SocProjectionResolver|null=null;
export function configureSocProjectionResolver(next:SocProjectionResolver){resolver=next;}
export async function readSocProjection(signal?:AbortSignal){let response:Response;try{response=await fetch('/v1/ui-projections/admin%3ASOC-01',{method:'GET',cache:'no-store',signal});}catch{return{ok:false as const,error_uid:'SOC-01-ERR-AUTH',reason_code:'SOC_PROJECTION_REQUEST_FAILED',correlation_id:'unresolved'}}const correlation_id=response.headers.get('x-correlation-id')??'unresolved',raw:unknown=await response.json().catch(()=>null);if(!response.ok){const body=typeof raw==='object'&&raw!==null?raw as Record<string,unknown>:null;return{ok:false as const,error_uid:'SOC-01-ERR-AUTH',reason_code:typeof body?.reason_code==='string'?body.reason_code:'SOC_PROJECTION_READ_FAILED',correlation_id:typeof body?.correlation_id==='string'?body.correlation_id:correlation_id}}if(!resolver)return{ok:false as const,error_uid:'SOC-01-ERR-UNDEFINED',reason_code:'SOC_PROJECTION_ADAPTER_NOT_BOUND',correlation_id};try{return{ok:true as const,projection:await resolver.resolve(raw),correlation_id}}catch{return{ok:false as const,error_uid:'SOC-01-ERR-UNDEFINED',reason_code:'SOC_PROJECTION_ADAPTER_REJECTED',correlation_id}}}
