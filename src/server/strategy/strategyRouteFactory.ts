import{NextRequest,NextResponse}from"next/server";import{executeStrategyDecision,type StrategyDecisionOperation}from"./strategyDecisionRuntime";
function headers(correlation_id:string){return{"x-correlation-id":correlation_id,"cache-control":"no-store"};}
export function strategyPost(operation_id:StrategyDecisionOperation){return async(r:NextRequest)=>{const current=r.headers.get("x-correlation-id"),correlation_id=current&&current.trim()?current:crypto.randomUUID();let payload:unknown;try{payload=await r.json();}catch{return NextResponse.json({reason_code:"INVALID_JSON_BODY",correlation_id},{status:400,headers:headers(correlation_id)});}const result=await executeStrategyDecision({operation_id,correlation_id,payload});if(!result.ok)return NextResponse.json({reason_code:result.reason_code,correlation_id},{status:result.status,headers:headers(correlation_id)});return NextResponse.json(result.value,{status:200,headers:headers(correlation_id)});};}
