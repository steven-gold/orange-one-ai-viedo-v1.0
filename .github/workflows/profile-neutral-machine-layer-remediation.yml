name: Profile Neutral Machine Layer Remediation

on:
  push:
    branches: [rebuild-v2.1.1]
    paths:
      - 'governance/ci/apply_profile_neutral_machine_layer.py'
      - '.github/workflows/profile-neutral-machine-layer-remediation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  remediate-and-verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONPYCACHEPREFIX: /tmp/acpos-governance-pycache
      BASELINE_SHA: ${{ github.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: rebuild-v2.1.1
      - name: Install verifier dependency
        run: python -m pip install --disable-pip-version-check 'PyYAML==6.0.2'
      - name: Inventory all retired global Stage-binding references before mutation
        shell: bash
        run: |
          set -euo pipefail
          echo '=== RETIRED_GLOBAL_STAGE_BINDING_REFERENCE_INVENTORY ==='
          grep -RInE 'targeted-stage01-stage02\.yml|validate_stage01_registry_binding\.py|STAGE01_ACTIVE_BINDING\.yaml' .github governance \
            --exclude='apply_profile_neutral_machine_layer.py' \
            --exclude='profile-neutral-machine-layer-remediation.yml' || true
          echo '=== END_REFERENCE_INVENTORY ==='
      - name: Compile corrected bootstrap then apply profile-neutral remediation
        shell: bash
        run: |
          # R4: baseline includes layout and selected-profile Stage-02 consumer migration.
          python - <<'PY'
          from pathlib import Path

          path = Path('governance/ci/apply_profile_neutral_machine_layer.py')
          source = path.read_text(encoding='utf-8')
          bad = "'))).decode())"
          good = "')).decode())"
          stripped = source.rstrip()
          if not stripped.endswith(bad):
              raise SystemExit('BLOCKED: expected one known bootstrap suffix defect was not found')
          fixed = stripped[:-len(bad)] + good + '\n'
          code = compile(fixed, str(path), 'exec')
          print('PASS: one known bootstrap parenthesis defect corrected in-memory; payload compiled')
          exec(code, {'__name__': '__main__', '__file__': str(path)})
          PY
